{
  "name": "invoice-crm-desktop",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "electron": "^31.0.0",
    "tree-kill": "^1.2.2"
  }
}
